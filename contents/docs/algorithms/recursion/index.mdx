---
title: Recursion
description: Learn about recursion, a fundamental concept in Data Structures and Algorithms.
---

![Recursion Example](/images/recursion-diagram.png)

## What is Recursion?

Recursion is a technique where a function calls itself to solve a smaller instance of the same problem.

It is typically used when:

- The problem can be broken down into smaller subproblems.
- Each decision reduces the input size until a base case is reached.

### How to Identify Recursion?

A problem is recursive if:

- We need to make `decisions` based on `choices`.
- Each choice leads to a smaller input or simpler version of the problem.
- There is a `clear base case` (a stopping condition) where recursion ends.
- The subproblems can be combined to form the `final solution`.

<Callout>👉 Choices + Decisions = Recursion</Callout>

## Example: Subsequence / Substring Generation

Consider the string: `const s = "abc";`

Recursion is about making binary choices at each step: `include` or `exclude` each character from the original string. Exploring all such decision paths results in all possible subsequences.

**Recursive Decision**

For each character in the string, we recursively decide to:

- `Include` the character.
- `Exclude` the character.

Generating All Subsequences: Recursive Tree Approach

| Output | a   | b   | c   |
| ------ | --- | --- | --- |
| ""     | ✗   | ✗   | ✗   |
| a      | ✓   | ✗   | ✗   |
| b      | ✗   | ✓   | ✗   |
| c      | ✗   | ✗   | ✓   |
| ab     | ✓   | ✓   | ✗   |
| bc     | ✗   | ✓   | ✓   |
| ac     | ✓   | ✗   | ✓   |
| abc    | ✓   | ✓   | ✓   |

✔ = character included  
✗ = character excluded

## Recursion LeetCode Problems

| Problem Title                                                                     | Difficulty | Description                                                                        |
| --------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------- |
| [Generate Parentheses](https://leetcode.com/problems/generate-parentheses/)       | Medium     | Generate all combinations of well-formed parentheses given n pairs.                |
| [Permutations](https://leetcode.com/problems/permutations/)                       | Medium     | Return all possible permutations of a list of distinct numbers.                    |
| [Permutations II](https://leetcode.com/problems/permutations-ii/)                 | Medium     | Return all unique permutations of a list that may contain duplicates.              |
| [Combinations](https://leetcode.com/problems/combinations/)                       | Medium     | Generate all possible combinations of k numbers from 1 to n.                       |
| [Subsets](https://leetcode.com/problems/subsets/)                                 | Medium     | Return all possible subsets (the power set) of a list of numbers.                  |
| [Subsets II](https://leetcode.com/problems/subsets-ii/)                           | Medium     | Return all possible subsets of numbers that may contain duplicates.                |
| [Combination Sum](https://leetcode.com/problems/combination-sum/)                 | Medium     | Find all unique combinations of candidates that sum to a target (reusing allowed). |
| [Combination Sum II](https://leetcode.com/problems/combination-sum-ii/)           | Medium     | Find all unique combinations of candidates that sum to a target (no reuse).        |
| [Combination Sum III](https://leetcode.com/problems/combination-sum-iii/)         | Medium     | Find all valid combinations of k numbers (1–9) that sum to n.                      |
| [Palindrome Partitioning](https://leetcode.com/problems/palindrome-partitioning/) | Medium     | Partition a string into substrings such that every substring is a palindrome.      |
| [Restore IP Addresses](https://leetcode.com/problems/restore-ip-addresses/)       | Medium     | Return all possible valid IP address combinations from a string of digits.         |
| [Binary Watch](https://leetcode.com/problems/binary-watch/)                       | Medium     | Return all possible times a binary watch could represent given turned-on LEDs.     |
